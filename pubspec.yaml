name: liquid_refraction_surface
description: "A focused Flutter package for liquid refraction surface effects."
version: 0.0.1
publish_to: "none"

environment:
  sdk: ^3.11.3
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_shaders: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

topics:
  - flutter
  - shader
  - refraction
  - effects

flutter:
  shaders:
    - shaders/liquid_refraction.frag
